# ローカル実行用。値は Vercel の環境変数からコピーしてください（.env.local は git 管理外）

# Neon PostgreSQL の接続文字列（careplan-delivery / kigen-kanri と共有）
DATABASE_URL=

# 入力・検索画面のログインパスワード（Vercel にも同名で設定）
SEARCH_PASSWORD=

# 旧 Google スプレッドシート（GAS）の検索用 URL。過去データ移行スクリプトでのみ使用
GAS_URL=
